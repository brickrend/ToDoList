// import { observer } from "mobx-react";
// import storeInstance from "../store/taskStore";
// import ToDoForm from "./ToDoForm";

// const TasksShow = () => {
//   const taskShow = storeInstance.tasks.map((task) => {
//     return <ToDoForm key={task.id} task={task} />;
//   });
//   return <div>{taskShow}</div>;
// };

// export default observer(TasksShow);
